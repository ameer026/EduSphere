# create a .env (edit value)
echo "GOOGLE_API_KEY=your_key_here" > .env

# install requirements
pip install -r requirements.txt

# quick syntax check (compiles all .py files)
python -c "import py_compile,glob; files = glob.glob('*.py') + glob.glob('pages/*.py'); [py_compile.compile(f, doraise=True) for f in files]"

# run the app
streamlit run 1_🏠_Home.py
